# 🧠 SimpleSNN - Spiking Neural Network in Java

[![Java](https://img.shields.io/badge/Java-8%2B-orange.svg)](https://www.java.com/)
[![License](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![No Dependencies](https://img.shields.io/badge/Dependencies-None-green.svg)]()

Простая реализация **спайковой нейронной сети** (SNN) с LIF-нейронами и STDP-обучением на чистой Java без внешних зависимостей.

## 🎯 Что это такое?

Спайковые нейронные сети — это третье поколение нейросетей, которые работают как настоящий мозг: нейроны общаются **импульсами (спайками)**, а не числами.

**Преимущества:**
- 🔋 В 1000-10000 раз энергоэффективнее обычных нейросетей
- ⚡ Естественная обработка временны́х данных
- 🧬 Биологически правдоподобное обучение (STDP)

## 📦 Быстрый старт

```bash
# Клонируем репозиторий
git clone https://github.com/yourusername/SimpleSNN.git
cd SimpleSNN

# Компилируем
javac src/SimpleSNN.java

# Запускаем
java -cp src SimpleSNN
```

## 🏗️ Архитектура

```
Входной слой (4 нейрона)
        ↓
   [8 синапсов с STDP]
        ↓
Выходной слой (2 нейрона LIF)
```

## 🧪 Компоненты

### LIF-нейрон (Leaky Integrate-and-Fire)

Модель нейрона по принципу **«ведро с дыркой»**:
- Вода (сигнал) наливается
- Одновременно вытекает через дырку (утечка)
- При переполнении — **СПАЙК!**

```java
boolean update(double input) {
    potential = potential * 0.9;  // Утечка
    potential += input;            // Накопление
    if (potential >= 1.0) {
        potential = 0;             // Сброс
        return true;               // СПАЙК!
    }
    return false;
}
```

### STDP (Spike-Timing-Dependent Plasticity)

Правило обучения **«кто первый — тот и причина»**:
- Нейрон A сработал → потом B сработал → **усиливаем** связь A→B
- Нейрон B сработал → потом A сработал → **ослабляем** связь

```java
void stdpUpdate(boolean pre, boolean post) {
    if (pre && post) weight += 0.1;      // LTP
    else if (pre)    weight -= 0.05;     // LTD
}
```

## 📊 Результаты

| Метрика | До обучения | После обучения |
|---------|-------------|----------------|
| Точность | ~50% | **100%** |

Задача: классификация паттернов
- Класс 0: `[1, 1, 0, 0]` (сигнал слева)
- Класс 1: `[0, 0, 1, 1]` (сигнал справа)

## 📁 Структура проекта

```
SimpleSNN/
├── src/
│   └── SimpleSNN.java    # Основной код
├── docs/
│   └── THEORY.md         # Теория SNN и STDP
├── examples/
│   └── output.txt        # Пример вывода
├── README.md
├── LICENSE
└── .gitignore
```

## 🔬 Демонстрации

Программа включает три демонстрации:

1. **demoLIF()** — работа LIF-нейрона, генерация спайков
2. **demoSTDP()** — изменение весов по правилу STDP
3. **demoClassification()** — обучение и классификация паттернов

## 📚 Теоретическая база

- **LIF-модель**: Gerstner & Kistler, "Spiking Neuron Models" (2002)
- **STDP**: Markram et al., Science (1997)
- **Нейроморфные чипы**: Intel Loihi, IBM TrueNorth

## 🚀 Возможные улучшения

- [ ] Добавить скрытый слой
- [ ] Реализовать временно́е кодирование (temporal coding)
- [ ] Визуализация спайков
- [ ] Более сложные задачи (MNIST, XOR)
- [ ] Экспорт весов в JSON

## 📄 Лицензия

MIT License — используйте свободно!

## 👤 Автор

**Студент группы 5140901/51501**  
СПбПУ Петра Великого  
Курс: Интеллектуальные системы

---

⭐ Если проект полезен — поставьте звёздочку!
